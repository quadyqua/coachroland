requests>=2.31
python-dotenv>=1.0
openai>=1.0
pillow>=10.0
mss>=9.0
numpy>=1.24
flask>=3.0
rapidocr-onnxruntime>=1.3   # free local OCR (lobby reader); avoids paid vision calls
# Optional GPU acceleration for the OCR readers (Windows, ~4x faster shop/lobby reads):
#   pip uninstall -y onnxruntime && pip install onnxruntime-directml
# localvision._engine auto-uses DirectML if present, else falls back to CPU onnxruntime.
