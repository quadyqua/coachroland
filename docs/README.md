# Screenshots

Drop the two screenshots referenced by the READMEs here:

- **`scout-api.png`** — the `/scout` endpoint executed in Swagger UI, showing a live
  `200` response with real comp data (`http://scout.localhost/docs` → GET /scout →
  Try it out → `quadyqua#NA1` → Execute). This is the "it works end-to-end" shot.
- **`scout-docs.png`** *(optional)* — the clean `/docs` overview page.

Capture with **Win+Shift+S**, save with these exact filenames, and the image links in
`README.md` and `k8s/README.md` will resolve.
